# Justified duplications (dry-audit dispositions) for scripts/check-duplication.ps1.
# Format: <16-hex sig hash>  <reason>
# Each hash is sha256[0:16] of an EXACT normalized duplicated block, taken from the
# detector's [sig:...] output. A waiver SELF-EXPIRES when the code changes (the hash
# changes), forcing re-justification instead of rotting open. These are idiomatic
# boilerplate that cannot be meaningfully DRY-ed: per-script settings that must live in
# each script's own scope, required CI steps, and per-function param blocks.

# Required GitHub Actions checkout boilerplate (actions/checkout + runs-on). Cannot be
# DRY-ed into a composite action without the coverage gate having to special-case it.
1c344bf48d54a3ed  workflow checkout boilerplate (readiness-gate.yml vs catalog-sync-check.yml)

# Standard checker bootstrap: Set-StrictMode + $ErrorActionPreference + Import-Module
# repo-root/audit-note-helpers + Resolve-RepoRoot. Set-StrictMode and $ErrorActionPreference
# must be set in each script's OWN scope (a shared function cannot set the caller's strict
# mode), so the bootstrap is intentionally self-contained per checker script.
139c2ed69fa3ac9f  checker bootstrap (StrictMode + module imports + Resolve-RepoRoot)
e439bc1c376f5d04  checker bootstrap (StrictMode + module imports + Resolve-RepoRoot)
9a110f8ffa80d5f7  checker bootstrap (StrictMode + module imports + Resolve-RepoRoot)
13cfd6f468330eae  checker bootstrap (StrictMode + module imports + Resolve-RepoRoot)
675e15163318ced7  checker bootstrap (StrictMode + module imports + Resolve-RepoRoot)
67edbacb095259e9  checker bootstrap (StrictMode + module imports + Resolve-RepoRoot)
bb2038a1bccf8cc2  checker bootstrap (StrictMode + module imports + Resolve-RepoRoot)
6ffaf6941ba3224a  per-script exit-code constants idiom (check-pr-text.ps1 vs check-comment-audit.ps1; ExitOk/ExitViolation/ExitInvocation)
52a6dcce9917f91d  checker bootstrap (check-lint.ps1 vs check-structural-conformance.ps1; both -RequireGitWorkTree)
2b0e08ec095ecd59  checker bootstrap (check-lint.ps1 vs check-structural-conformance.ps1; both -RequireGitWorkTree)
76c64dc0e6ede321  checker bootstrap (check-lint.ps1 vs check-structural-conformance.ps1; both -RequireGitWorkTree)
76f9089c1e223033  checker bootstrap (check-lint.ps1 vs check-structural-conformance.ps1; both -RequireGitWorkTree)
311d2194d0584220  checker bootstrap (check-lint.ps1 vs check-structural-conformance.ps1; both -RequireGitWorkTree)
81dafde48bda0fda  checker bootstrap (check-lint.ps1 vs check-structural-conformance.ps1; both -RequireGitWorkTree)

# Per-function boilerplate in audit-note-helpers.psm1: CmdletBinding + the shared
# (RepoRoot, CommitSha) param block + the Invoke-AuditGit rev-parse pattern, repeated
# across the small git-accessor functions. Param blocks are per-function, not extractable.
1d8d3e053855a8cf  audit-note-helpers per-function param-block / git-runner boilerplate

# Per-checker test scaffolding shared by the two near-twin scan-checker self-tests
# (check-no-panel-artifacts.tests.ps1 vs check-playbook-refs.tests.ps1): the file-scope
# setup header ($repoRoot/$checker/$script:Pass/$script:Fail), the per-file fixture writer
# (New-Item + Set-Content, with divergent commit behavior), and the checker invoker
# (& pwsh -File $checker -RepoRoot $Dir; capture ExitCode + Output). Each references its
# OWN file-scope $checker and fixture dirs, so the helpers are intentionally self-contained
# per test file - the same disposition as the per-script checker-bootstrap waivers above.
9fae694941a8a6b7  per-checker test scaffolding (npa vs playbook-refs self-test)
39c2cc2ace307f0e  per-checker test scaffolding (npa vs playbook-refs self-test)
2ce6ef6e7609a9f7  per-checker test scaffolding (npa vs playbook-refs self-test)

# Parallel staged-mode gate logic between the two sibling pre-commit gates
# (check-comment-audit.ps1 vs check-post-code-change.ps1): the acceptable-parents
# amend-handling (rev-parse HEAD / HEAD^ under PANEL_GATE_AMEND), the staged-receipt
# read (-WorktreeReceipt disk vs `git show :path`), the candidate-parent validation
# loop, and the `if (-not $BaseRef)` invocation guard. These are structural scaffolding
# (parent-resolution, receipt-read, validation loop), NOT the gates' decision logic, which
# stays distinct (Test-AuditFile vs Test-PanelLedger). Extracting them would couple two
# independently-evolving gates for marginal line savings; each gate stays self-contained
# and independently auditable. Same self-contained-per-gate disposition as the
# checker-bootstrap waivers above.
93c72ef430c33728  parallel staged-gate logic (check-comment-audit vs check-post-code-change)
268d2c79c3ba63aa  parallel staged-gate logic (check-comment-audit vs check-post-code-change)
4633aee39592e3cd  parallel staged-gate logic (check-comment-audit vs check-post-code-change)
efad131936ee9af6  parallel staged-gate logic (check-comment-audit vs check-post-code-change)
ee566c7d0f352280  parallel staged-gate logic (check-comment-audit vs check-post-code-change)

# fu5 R2 machine-path gate (scripts/check-no-machine-paths.ps1 + machine-path-check.yml + its tests): shares
# the modern-gate scaffold with check-pr-text.ps1 (param tail + dot-source guard + StrictMode + exit-code
# constants), the checker bootstrap with check-no-panel-artifacts.ps1 (RepoRoot param + Resolve-RepoRoot),
# the pull_request workflow boilerplate with pr-gate-check.yml, and the test-harness preamble with
# check-commit-message.tests.ps1. Each is a per-file idiom that cannot be DRY-ed (StrictMode/exit constants
# must live in each script's own scope; CI steps must be inline for the coverage gate).
7b3e0924cb1ff799  pull_request workflow boilerplate (machine-path-check.yml vs pr-gate-check.yml)
2ea359910d9b7a46  pull_request workflow boilerplate (machine-path-check.yml vs pr-gate-check.yml)
1029b3b7d1366581  modern-gate dot-source guard + StrictMode scaffold (check-no-machine-paths.ps1 vs check-pr-text.ps1)
ee156ddc769f9847  modern-gate dot-source guard + StrictMode scaffold (check-no-machine-paths.ps1 vs check-pr-text.ps1)
3252302cfea3e586  modern-gate dot-source guard + StrictMode scaffold (check-no-machine-paths.ps1 vs check-pr-text.ps1)
b46ba1e1a27a2eca  modern-gate dot-source guard + StrictMode scaffold (check-no-machine-paths.ps1 vs check-pr-text.ps1)
141f3004a982ee06  modern-gate dot-source guard + StrictMode scaffold (check-no-machine-paths.ps1 vs check-pr-text.ps1)
2a28b3d3f888bed3  modern-gate dot-source guard + StrictMode scaffold (check-no-machine-paths.ps1 vs check-pr-text.ps1)
d506aa39b06e628e  checker bootstrap (check-no-machine-paths.ps1 vs check-no-panel-artifacts.ps1; RepoRoot param + Resolve-RepoRoot)
077606b1f9541a68  test-harness preamble (check-no-machine-paths.tests.ps1 vs check-commit-message.tests.ps1; Requires + StrictMode + test-common)

# fu5-size-reduction PR-1 automation-identity gate (scripts/check-no-automation-identity.ps1 + its tests): shares the
# range-mode commit-metadata gate scaffold with check-commit-message.ps1 - the per-script exit-code constants + the
# Invoke-Git fail-closed wrapper, the findings output-tail (-Json record / per-finding ::error / exit), and the
# test-harness Invoke-Checker preamble (& pwsh -File $checker -RepoRoot -BaseRef -HeadRef; capture ExitCode + Output).
# Each is a per-file idiom that cannot be DRY-ed (StrictMode/exit constants must live in each script's own scope; the
# Invoke-Git wrapper closes over the per-script $RepoRoot); the gates' decision logic (Get-IdentityFindings vs
# Get-MessageFindings) stays distinct. Same self-contained-per-gate disposition as the machine-path waivers above.
ff944910b2388c7b  exit-code constants + Invoke-Git idiom (check-no-automation-identity.ps1 vs check-commit-message.ps1)
21ca21c0fec2fb5f  exit-code constants + Invoke-Git idiom (check-no-automation-identity.ps1 vs check-commit-message.ps1)
9b800c62adcd68cf  exit-code constants + Invoke-Git idiom (check-no-automation-identity.ps1 vs check-commit-message.ps1)
dbda1c12d98b66c8  exit-code constants + Invoke-Git idiom (check-no-automation-identity.ps1 vs check-commit-message.ps1)
2fb1ad387b83e718  exit-code constants + Invoke-Git idiom (check-no-automation-identity.ps1 vs check-commit-message.ps1)
b0ed3b7dbfdac471  exit-code constants + Invoke-Git idiom (check-no-automation-identity.ps1 vs check-commit-message.ps1)
a4c4f7fdb2c8755d  exit-code constants + Invoke-Git idiom (check-no-automation-identity.ps1 vs check-commit-message.ps1)
22f09684fad67d55  exit-code constants + Invoke-Git idiom (check-no-automation-identity.ps1 vs check-commit-message.ps1)
b37b5149392dd589  exit-code constants + Invoke-Git idiom (check-no-automation-identity.ps1 vs check-commit-message.ps1)
989874d2a779f324  findings output-tail idiom (check-no-automation-identity.ps1 vs check-commit-message.ps1; -Json record / ::error / exit)
6c926528d6eab271  findings output-tail idiom (check-no-automation-identity.ps1 vs check-commit-message.ps1; -Json record / ::error / exit)
765a7f7f93882e96  findings output-tail idiom (check-no-automation-identity.ps1 vs check-commit-message.ps1; -Json record / ::error / exit)
24b99fd7c6570c31  test-harness Invoke-Checker preamble (check-no-automation-identity.tests.ps1 vs check-commit-message.tests.ps1)
f65f9946fc12288a  test-harness Invoke-Checker preamble (check-no-automation-identity.tests.ps1 vs check-commit-message.tests.ps1)
c2ce9b379007c5ac  checker bootstrap (check-no-automation-identity.ps1 vs check-commit-message.ps1; CmdletBinding + param)
648e47661b884633  checker bootstrap (check-no-automation-identity.ps1 vs check-commit-message.ps1; param tail + StrictMode)
59a631da0ec886f6  checker bootstrap (check-no-automation-identity.ps1 vs check-commit-message.ps1; param tail + StrictMode)
c872701255a6780f  checker bootstrap (check-no-automation-identity.ps1 vs check-commit-message.ps1; StrictMode + Import-Module repo-root)
c4b154c8c8ab6076  checker bootstrap (check-no-automation-identity.ps1 vs check-comment-audit.ps1; StrictMode + Import-Module + Resolve-RepoRoot)
b20021a1db77d4ca  checker bootstrap (check-no-automation-identity.ps1 vs check-comment-audit.ps1; StrictMode + Import-Module + Resolve-RepoRoot)
59d57722091af554  checker bootstrap (check-no-automation-identity.ps1 vs check-comment-audit.ps1; StrictMode + Import-Module + Resolve-RepoRoot)
b858d5fbd58681df  checker bootstrap (check-no-automation-identity.ps1 vs check-commit-message.ps1; CmdletBinding + param head)
7e8c74e6c4539059  pull_request workflow boilerplate (automation-identity-check.yml vs machine-path-check.yml; on/runs-on/checkout)
581e65fcb5027fda  pull_request workflow boilerplate (automation-identity-check.yml vs machine-path-check.yml; on/runs-on/checkout)
91b930c5e42576fc  pull_request workflow boilerplate (automation-identity-check.yml vs machine-path-check.yml; on/runs-on/checkout)
46162049bec9d8cb  gate-runner -Verify guard wraps the existing findings.csv append logic (intentional conditional skip; block matches the pre-Verify version it guards)
02f54eb74012f296  gate-runner -Verify guard wraps the existing findings.csv append logic (intentional conditional skip)
0a4d178766eb2b83  gate-runner -Verify guard wraps the existing findings.csv append logic (intentional conditional skip)
5214170777f3060c  CI-wrapper bootstrap (run-quality-gate-ci.ps1 vs check-diff-consistency-ci.ps1; StrictMode + Import-Module + Resolve-RepoRoot fail-closed)
3961d16ce29875af  CI-wrapper bootstrap (run-quality-gate-ci.ps1 vs check-diff-consistency-ci.ps1; StrictMode + Import-Module + Resolve-RepoRoot fail-closed)
404a92dea49772ab  gate-runner -Verify append block (mkdir moved inside rows>0 guard; heuristic flags the modified block as duplicating its pre-edit version - a modify, not a copy-paste)

# check-instructions-refs.ps1 deliberately mirrors the proven check-playbook-refs.ps1 (the panel-endorsed
# single-responsibility ref-checker pattern): the per-script bootstrap (CmdletBinding + param + Resolve-RepoRoot
# try/catch), the per-script exit-code constants, the Write-Invocation/Write-Violation helpers, and the
# violations-print-and-exit tail are idiomatic boilerplate shared by every check-*.ps1 and cannot be DRY-ed
# without diverging from all siblings.
a9d259f7512d204f  checker bootstrap (check-instructions-refs.ps1 vs check-playbook-refs.ps1; CmdletBinding + param + Resolve-RepoRoot)
469c3daa323fa77a  checker bootstrap (check-instructions-refs.ps1 vs check-playbook-refs.ps1; CmdletBinding + param + Resolve-RepoRoot)
11493ce129191f2f  checker bootstrap (check-instructions-refs.ps1 vs check-playbook-refs.ps1; CmdletBinding + param + Resolve-RepoRoot)
4a60d4656286ce60  checker bootstrap (check-instructions-refs.ps1 vs check-playbook-refs.ps1; exit-code constants + Write helpers)
3c51911c3c6af7e0  checker bootstrap (check-instructions-refs.ps1 vs check-playbook-refs.ps1; exit-code constants + Write helpers)
86cd0bc351230e3d  checker bootstrap (check-instructions-refs.ps1 vs check-playbook-refs.ps1; exit-code constants + Write helpers)
c64c7289d16ad8b4  checker bootstrap (check-instructions-refs.ps1 vs check-playbook-refs.ps1; exit-code constants + Write helpers)
63c48c81b2980c0f  checker bootstrap (check-instructions-refs.ps1 vs check-playbook-refs.ps1; exit-code constants + Write helpers)
d052fafe0c7e4b9d  checker bootstrap (check-instructions-refs.ps1 vs check-playbook-refs.ps1; exit-code constants + Write helpers)
2e1cfc8f8ab2a98b  checker bootstrap (check-instructions-refs.ps1 vs check-playbook-refs.ps1; exit-code constants + Write helpers)
dbb84938c73b4548  checker bootstrap (check-instructions-refs.ps1 vs check-playbook-refs.ps1; exit-code constants + Write helpers)
d8b8e1856de9802e  checker tail (check-instructions-refs.ps1 vs check-playbook-refs.ps1; violations-print-and-exit idiom)
267b738064a3fefa  checker tail (check-instructions-refs.ps1 vs check-playbook-refs.ps1; violations-print-and-exit idiom)
521af4e503998427  checker tail (check-instructions-refs.ps1 vs check-playbook-refs.ps1; violations-print-and-exit idiom)
# check-instructions-refs.tests.ps1 shares the standalone self-test harness (New-*Repo / Add-RepoFile / Run
# helpers) with check-playbook-refs.tests.ps1 - the established per-checker test scaffolding pattern.
44ff9d169102b894  per-checker test scaffolding (check-instructions-refs.tests.ps1 vs check-playbook-refs.tests.ps1; self-test harness helpers)
8db445a8f0b8a7da  per-checker test scaffolding (check-instructions-refs.tests.ps1 vs check-playbook-refs.tests.ps1; self-test harness helpers)
efb9f3e6e6448420  per-checker test scaffolding (check-instructions-refs.tests.ps1 vs check-playbook-refs.tests.ps1; self-test harness helpers)
4236c7e1abd8b853  per-checker test scaffolding (check-instructions-refs.tests.ps1 vs check-playbook-refs.tests.ps1; self-test harness helpers)
89daab4c04509df1  per-checker test scaffolding (check-instructions-refs.tests.ps1 vs check-playbook-refs.tests.ps1; self-test harness helpers)
3e62b8e8f0a4c966  per-checker test scaffolding (check-instructions-refs.tests.ps1 vs check-playbook-refs.tests.ps1; self-test harness helpers)
cb1c6c6001680184  per-checker test scaffolding (check-instructions-refs.tests.ps1 vs check-playbook-refs.tests.ps1; self-test harness helpers)