# HIGH-TIER catalog slugs

GENERATED FILE - do not hand-edit. Regenerated by `scripts/sync-critical-rules.ps1` or its bash twin `scripts/sync-critical-rules.sh` from `pattern-catalog.md`.

Catalog content hash: `4e6d0ff9ac175769abbcff28c3daff0c6fad078f`

Every HIGH-tier `review-pass-only` slug below MUST appear in `core_rules_acknowledged` on every commit with per-site `file:line:disposition` citations (see `panel-policy.md` §Per-rule acknowledgement for full schema).

## Slugs requiring acknowledgement

### `doc-impl-mismatch`

For every modified XML doc comment, README/docs/playbook text, or PR description claim: read each concrete prose claim and verify the code actually does what it says.

Canonical rule definition: see `pattern-catalog.md` row `doc-impl-mismatch`.

### `comment-necessity`

**CITATION-FIRST:** Every NEW or substantively rewritten comment in the diff MUST cite an `approval_turn:` field in the §2.6 audit ledger (in this instructions repo a LOCAL git note; the gitignored...

Canonical rule definition: see `pattern-catalog.md` row `comment-necessity`.

### `prefer-async-suffix`

For every NEW sync call added in the diff INSIDE a genuinely async method - defined as: the enclosing method declaration has the `async` keyword OR the method body contains `await` somewhere (this ...

Canonical rule definition: see `pattern-catalog.md` row `prefer-async-suffix`.

### `event-ordering-test-subscription`

For every NEW test in the diff that asserts ORDERING between an EVENT (e.g., `StateChanged?.Invoke()`, `PropertyChanged`, `INotifyPropertyChanged.PropertyChanged`) and a SUBJECT-ACTION (e.g., a TCS...

Canonical rule definition: see `pattern-catalog.md` row `event-ordering-test-subscription`.

### `event-completion-fault-isolation`

For every NEW or modified writer method in the diff that BOTH (a) invokes a public event (e.g., `StateChanged?.Invoke()`, `PropertyChanged?.Invoke(...)`, custom multi-cast delegate) OR a caller-sup...

Canonical rule definition: see `pattern-catalog.md` row `event-completion-fault-isolation`.

### `code-comment-mismatch`

For every NEW or hunk-adjacent inline/block code comment or config-file comment (`//`, `/* */`, `.csproj`/`.props`/MSBuild config comments) in or next to modified lines: verify the comment text mat...

Canonical rule definition: see `pattern-catalog.md` row `code-comment-mismatch`.

### `silent-fallback-loses-info`

For every NEW `??` / `?? default` / `try/catch { return null; }` / `try/catch { return ""; }` / `if (...) return defaultValue;` / `catch (Exception) { /* ignored */ }` added in the diff where the f...

Canonical rule definition: see `pattern-catalog.md` row `silent-fallback-loses-info`.

### `nullable-optional-callback-with-surprising-no-op-default`

For every NEW callback parameter (`Func<...>?`, `Action<...>?`, custom delegate) declared as nullable with a default value (`= null`) in a public/internal API: verify that the null-default behavior...

Canonical rule definition: see `pattern-catalog.md` row `nullable-optional-callback-with-surprising-no-op-default`.

### `output-format-incompatible-with-new-renderer`

For every NEW or modified site in the diff that builds an output string (`StringBuilder.AppendLine` + bullet glyphs, `\n`-separated lines, tab-aligned columns, indented sub-items, ANSI color escape...

Canonical rule definition: see `pattern-catalog.md` row `output-format-incompatible-with-new-renderer`.

### `user-facing-text-fragment-case-mismatch-on-concatenation`

For every NEW user-facing text composition in the diff (banner messages, dialog text, toast notifications, modal titles + bodies, button labels, error/info/warning banner content) that concatenates...

Canonical rule definition: see `pattern-catalog.md` row `user-facing-text-fragment-case-mismatch-on-concatenation`.

### `callback-swallows-exception-the-caller-expects-to-propagate`

For every NEW or modified callback IMPLEMENTATION (lambda / local function / method passed as `Func<...>` / `Action<...>` / custom delegate) in the diff whose body contains `catch (XException) { re...

Canonical rule definition: see `pattern-catalog.md` row `callback-swallows-exception-the-caller-expects-to-propagate`.

### `dispatcher-fire-and-forget-where-completion-must-be-awaited`

For every NEW or modified method whose name OR XML doc OR call-site context implies SEQUENTIAL DEPENDENCY between dispatched steps (verbs like "reload", "reset", "rotate", "close-and-reopen", "drai...

Canonical rule definition: see `pattern-catalog.md` row `dispatcher-fire-and-forget-where-completion-must-be-awaited`.

### `dispose-leak`

For every NEW owned `IDisposable`/`IAsyncDisposable`, pooled buffer, writer, semaphore, stream, or `SafeHandle`/native resource in the diff, verify it is released on ALL exit paths.

Canonical rule definition: see `pattern-catalog.md` row `dispose-leak`.

### `async-blocking-on-captured-syncctx`

For every NEW `.Result` / `.Wait()` / `.GetAwaiter().GetResult()` in the diff on a `Task` / `Task<T>` whose continuation may run on a captured synchronization context (UI thread, ASP.NET classic SC...

Canonical rule definition: see `pattern-catalog.md` row `async-blocking-on-captured-syncctx`.

### `async-void-not-event-handler`

For every NEW `async void` method declaration in the diff: verify the signature matches a real event-handler shape (`void Foo(object? sender, EventArgs e)`, Blazor `void OnX(SomeEventArgs e)`, MAUI...

Canonical rule definition: see `pattern-catalog.md` row `async-void-not-event-handler`.

### `fire-and-forget-discard-unobserved`

For every NEW unawaited `Task`/`ValueTask` call in the diff (the CS4014 surface) plus any `_ =` discard, verify the task's exceptions are observed.

Canonical rule definition: see `pattern-catalog.md` row `fire-and-forget-discard-unobserved`.

### `task-completedtask-from-async-named-method`

For every NEW method in the diff whose declaration returns `Task` / `Task<T>` / `ValueTask` / `ValueTask<T>` AND whose body returns `Task.CompletedTask` / `Task.FromResult(...)` / `ValueTask.Comple...

Canonical rule definition: see `pattern-catalog.md` row `task-completedtask-from-async-named-method`.

### `partial-cleanup-on-async-failure-path`

For every NEW method in the diff that (a) registers N resources / completion tokens / cancellation registrations / shared-state entries BEFORE entering an `await` loop, AND (b) the await loop can t...

Canonical rule definition: see `pattern-catalog.md` row `partial-cleanup-on-async-failure-path`.

### `silent-cancellation-via-early-return-vs-throw`

For every NEW `if (cancellationToken.IsCancellationRequested) { return; }` pattern in the diff inside a method returning `Task` / `Task<T>` / `ValueTask` / `ValueTask<T>`: verify the caller can dis...

Canonical rule definition: see `pattern-catalog.md` row `silent-cancellation-via-early-return-vs-throw`.

### `async-loop-no-intermediate-cancellation-check`

For every NEW or modified async method in the diff that takes a `CancellationToken` parameter AND contains `foreach` / `for` / `while` loops where the loop body either (a) awaits an operation that ...

Canonical rule definition: see `pattern-catalog.md` row `async-loop-no-intermediate-cancellation-check`.

### `await-throwable-async-call-without-handler-in-teardown-flow`

For every NEW awaited JS/focus/IO call in a teardown, dispose, close, `OnAfterRender`, render-restoration, or disconnect path, verify expected exceptions are handled narrowly.

Canonical rule definition: see `pattern-catalog.md` row `await-throwable-async-call-without-handler-in-teardown-flow`.

### `log-exception-via-message-loses-stack-and-inner-context`

For every NEW `_logger.Warning` / `_logger.Error` / `TraceLogger.Warning` / `TraceLogger.Error` / equivalent diagnostic log call in the diff that interpolates `{ex.Message}` (or `ex.Message` via co...

Canonical rule definition: see `pattern-catalog.md` row `log-exception-via-message-loses-stack-and-inner-context`.

### `deferred-throw-resets-stack-trace-use-exceptiondispatchinfo`

For every NEW pattern in the diff that captures an exception in a local variable and rethrows it later via `throw <varName>;` - typically in a deferred-throw / catch-and-defer / capture-then-cleanu...

Canonical rule definition: see `pattern-catalog.md` row `deferred-throw-resets-stack-trace-use-exceptiondispatchinfo`.

### `full-scan-against-new-rule-not-triggered-after-bot-finding`

**PROCESS RULE.** For every catalog change (new slug OR enhancement to an existing slug's audit-method) that was triggered by a bot finding on the active PR: the agent MUST execute the new/enhanced...

Canonical rule definition: see `pattern-catalog.md` row `full-scan-against-new-rule-not-triggered-after-bot-finding`.

### `razor-lambda-eventcallback-drops-task`

For every NEW Razor block lambda or expression lambda assigned to an event handler attribute (`@onclick`, `@onchange`, `@onkeydown`, `@oninput`, `@onsubmit`, etc.) in a `.razor` file whose BODY inv...

Canonical rule definition: see `pattern-catalog.md` row `razor-lambda-eventcallback-drops-task`.

### `guard-predicate-incomplete-after-multi-signal-redesign`

For every NEW or modified click-guard / disable-predicate / aria-disabled expression / early-return condition in the diff that gates a destructive or state-mutating action AND the surrounding featu...

Canonical rule definition: see `pattern-catalog.md` row `guard-predicate-incomplete-after-multi-signal-redesign`.

### `user-facing-message-content-regression-after-callback-extraction`

For every NEW or modified call site in the diff that REPLACES a caller-supplied delegate (`Func<bool, ...> confirmAsync`, `Action<DialogContext>`, etc.) with a static/no-op delegate (`(_, _) => Tas...

Canonical rule definition: see `pattern-catalog.md` row `user-facing-message-content-regression-after-callback-extraction`.

### `failure-detail-collected-but-not-surfaced`

For every NEW or modified method in the diff that collects per-item failure information into a list / dictionary (`List<(string, string)> failed`, `Dictionary<string, Exception> errors`, `IReadOnly...

Canonical rule definition: see `pattern-catalog.md` row `failure-detail-collected-but-not-surfaced`.

### `post-refactor-state-tracking-loss-of-sticky-flag`

For every refactor / extraction in the diff that REPLACES a previously-monolithic UI component's state-tracking with a derived signal (e.g., session-scoped `_settingsDirty` flag replaced with `HasD...

Canonical rule definition: see `pattern-catalog.md` row `post-refactor-state-tracking-loss-of-sticky-flag`.

### `parent-prop-narrowing-occludes-component-wide-signal`

For every NEW or modified parent-to-child component parameter binding in the diff that PASSES a NARROWED version of a component/session-wide signal (e.g., `IsActionBlocked="@(IsActionBlocked && !Co...

Canonical rule definition: see `pattern-catalog.md` row `parent-prop-narrowing-occludes-component-wide-signal`.

### `stacked-pr-fix-propagation-gap-during-rebase`

**PROCESS RULE.** For every stack of PRs (foundation → polish → feature) where a defect found in the foundation PR is fixed on a HIGHER PR in the stack via a fix commit that is NOT cherry-picked ba...

Canonical rule definition: see `pattern-catalog.md` row `stacked-pr-fix-propagation-gap-during-rebase`.

### `implementation-to-commit-transition-bypasses-user-approval`

**PROCESS RULE.** At the implementation → git transition (tests green, lint clean, optionally PR-quality-gate `core_rules_acknowledged` block emitted), the agent's FIRST action MUST be mechanical c...

Canonical rule definition: see `pattern-catalog.md` row `implementation-to-commit-transition-bypasses-user-approval`.

### `razor-css-isolation-cross-file-class-reference`

For every NEW or modified `.razor` markup file that references a CSS class name in its `class="..."` attributes: verify the class name is either (a) defined in the SAME component's `.razor.css` fil...

Canonical rule definition: see `pattern-catalog.md` row `razor-css-isolation-cross-file-class-reference`.

### `aria-disabled-without-disabled-attribute`

For every NEW or modified interactive element (`<button>`, `<a>`, `<input>`, `<select>`, `<textarea>`, custom Razor button component) in `.razor` / `.html` / `.cshtml` files that has `aria-disabled...

Canonical rule definition: see `pattern-catalog.md` row `aria-disabled-without-disabled-attribute`.

### `readonly-interface-leaks-backing-mutable-collection`

For every NEW or modified public/internal API surface in the diff that exposes a property, return value, event-args field, or test-fake-stored value typed as a read-only collection interface (`IRea...

Canonical rule definition: see `pattern-catalog.md` row `readonly-interface-leaks-backing-mutable-collection`.

### `interface-event-non-nullable-vs-impl-nullable-mismatch`

For every NEW or modified event declaration in the diff on an `interface` / `abstract class` whose nullable-reference-type annotation does NOT match the concrete implementation's annotation: verify...

Canonical rule definition: see `pattern-catalog.md` row `interface-event-non-nullable-vs-impl-nullable-mismatch`.

### `razor-empty-css-selector-foiled-by-whitespace-text-nodes`

For every NEW or modified `.razor.css` / `.css` file in the diff that uses the `:empty` pseudo-class selector (e.g., `.toolbar-leading:empty { display: none; }` or `.filter-error:empty { display: n...

Canonical rule definition: see `pattern-catalog.md` row `razor-empty-css-selector-foiled-by-whitespace-text-nodes`.

### `razor-button-missing-type-defaults-to-submit`

For every NEW or modified `<button>` element rendered inside a Blazor `<EditForm>` or a raw HTML `<form>` in a `.razor` file - OR any `<button>` that newly falls inside a NEW or modified `<EditForm...

Canonical rule definition: see `pattern-catalog.md` row `razor-button-missing-type-defaults-to-submit`.

### `regex-instance-without-matchtimeout-allows-catastrophic-backtracking`

For every NEW `new Regex(...)` constructor call in the diff, OR for every method that accepts a `Regex` parameter from a caller, verify the regex was constructed with a finite `MatchTimeout` (e.g.,...

Canonical rule definition: see `pattern-catalog.md` row `regex-instance-without-matchtimeout-allows-catastrophic-backtracking`.

### `namespace-mismatch-symbol-referenced-without-using`

For every NEW symbol reference in the diff (a type name, an extension method call, an `@using` directive in Razor) where the file's namespace cannot reach the symbol's declaring namespace through C...

Canonical rule definition: see `pattern-catalog.md` row `namespace-mismatch-symbol-referenced-without-using`.

### `facade-discards-inner-result-discriminator`

For every NEW or modified wrapper / facade / adapter method in the diff that wraps a result-discriminator-returning inner call (`Result<T>`, custom `Outcome` enum, `(bool Success, T Value)` tuple, ...

Canonical rule definition: see `pattern-catalog.md` row `facade-discards-inner-result-discriminator`.

### `record-non-nullable-ctor-validation`

For every NEW `record` / `record struct` / `class` with a primary constructor declaration in the diff that has one or more non-nullable reference-type parameters (`string`, `IReadOnlyList<T>`, cust...

Canonical rule definition: see `pattern-catalog.md` row `record-non-nullable-ctor-validation`.

### `state-read-stale-after-event-publication`

For every NEW pattern in the diff where (a) one method PUBLISHES an event / raises a notification / completes a TCS / dispatches to a Fluxor action that has a corresponding event handler / subscrib...

Canonical rule definition: see `pattern-catalog.md` row `state-read-stale-after-event-publication`.

### `least-privilege-audit-required-on-visibility-delta`

**PROCESS RULE.** For every commit whose diff contains a visibility / export / mutability surface delta - adds a public/exported type or member, widens visibility, removes `sealed`/`final`, adds or...

Canonical rule definition: see `pattern-catalog.md` row `least-privilege-audit-required-on-visibility-delta`.

### `intent-driven-testing-required-on-test-or-SUT-delta`

**PROCESS RULE.** For every commit whose diff contains EITHER (a) NEW or modified test files (paths matching `**/*Tests.*`, `**/test_*`, `**/*.spec.*`, `**/__tests__/**`, or framework-specific patt...

Canonical rule definition: see `pattern-catalog.md` row `intent-driven-testing-required-on-test-or-SUT-delta`.

### `pre-impl-missed-implementation-planning-on-nontrivial-change`

**PROCESS RULE.** For every commit whose POST-CODE-CHANGE LEDGER `gates.pre-impl-trigger-detections.implementation-planning` is `yes` (G6 detected the implementation-planning trigger because the pr...

Canonical rule definition: see `pattern-catalog.md` row `pre-impl-missed-implementation-planning-on-nontrivial-change`.

### `implementation-phase-missed-playbook-required-by-pre-impl`

**PROCESS RULE.** Meta-rule covering the 4 cycle-3-scope playbooks with both `pre-impl-playbook-decisions` AND `playbook-invocations` LEDGER entries: implementation-planning, library-restructure, d...

Canonical rule definition: see `pattern-catalog.md` row `implementation-phase-missed-playbook-required-by-pre-impl`.

### `library-restructure-required-on-folder-namespace-move-in-diff`

**PROCESS RULE.** POST-IMPL companion to rule 3.

Canonical rule definition: see `pattern-catalog.md` row `library-restructure-required-on-folder-namespace-move-in-diff`.

### `implementation-planning-required-on-nontrivial-final-diff`

**PROCESS RULE.** POST-IMPL companion to rule 2.

Canonical rule definition: see `pattern-catalog.md` row `implementation-planning-required-on-nontrivial-final-diff`.

### `committed-gate-skipped-or-unilaterally-carved-out`

**PROCESS RULE.** Review THIS session's branch + conversation (no artifact records a verbal promise, so anchor to what the panel sees) for the three momentum-driven gate-evasion shapes: (1) GATE-SK...

Canonical rule definition: see `pattern-catalog.md` row `committed-gate-skipped-or-unilaterally-carved-out`.

### `input-edge-case-enumeration`

For every CHANGED entry point in the diff that accepts input (function / method parameters, environment variables, file arguments, request fields, stdin, CLI args): enumerate EACH input surface aga...

Canonical rule definition: see `pattern-catalog.md` row `input-edge-case-enumeration`.

### `sibling-variant-consistency`

For every change that touches ONE member of a set of N sibling constructs sharing a SHAPE (sibling regexes / validators / parsers / path handlers / format matchers / serializers that match the same...

Canonical rule definition: see `pattern-catalog.md` row `sibling-variant-consistency`.

### `loop-silently-drops-failed-items`

For every NEW `foreach` / `for` / `for..of` / `.map` / `.filter` loop in the diff that builds a result collection from already-validated input AND skips an element on a sub-step failure via `contin...

Canonical rule definition: see `pattern-catalog.md` row `loop-silently-drops-failed-items`.

## Acknowledgement template (per-slug)

```yaml
core_rules_acknowledged:
  - slug: <slug>
    status: <applied | not-applicable>
    evidence:
      per_site_citations:
        - file: <path>
          line: <int or range>
          disposition: <rename | extract | remove | restore | keep-because>
          keep_reason: <rationale; MUST add information beyond comment text>
      diff_metric_check: <rg-violation count vs per_site_citations count>
    rationale: <≤30 words; required for not-applicable>
```